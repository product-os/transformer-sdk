import type { InputManifest, Result } from '<%= sdk.name %>'
import type { <%= requiredInterfaces %> } from './types'
<% for (const func of functions) { %>
export async function <%= func.name %> (input: InputManifest<<%= func.inputType %>>): Promise<Result<<%= func.outputType %>>[]> {
    // TODO: implement transformation
}
<% } %>
