import { ContractDefinition } from '<%= sdk.name %>'
<% for (const type of types) { %>
<%- type.data %>
export interface <%= type.contractInterfaceName %> extends ContractDefinition<<%= type.dataInterfaceName %> > {
    type: '<%= type.handle %>'
}
<% } %>
